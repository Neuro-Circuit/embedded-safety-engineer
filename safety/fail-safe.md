# Fail Safe
For safety-relevant failures: detect -> contain -> record safely -> transition to a
defined safe state -> recover/restart if justified. Never invent a domain-specific
safe state without requirements.
