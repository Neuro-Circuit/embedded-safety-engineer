# RTOS
Review priorities, stack sizes, blocking, mutex/semaphore choice, priority inversion,
deadlock, starvation, queue capacity, timers, ISR-safe APIs, scheduler assumptions,
and watchdog interaction.
