# Communications Security
Validate framing and lengths, reject malformed input safely, authenticate/integrity-protect
when required, consider replay, bound parser work, rate-limit expensive operations,
handle timeouts, and never blindly trust device-reported sizes/state.
