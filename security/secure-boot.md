# Secure Boot
Review the chain from immutable/root trust through bootloader to application. Verify
authenticity before execution, protect verification keys, prevent unauthorized downgrade
where required, and define failure/recovery behavior.
